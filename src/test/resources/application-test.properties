# ==================== RAG Test Configuration ====================
# Consolidated RAG properties for unit-test contexts.
# No real database or API key required — all dependencies are mocked.

rag.retrieval.top-k=3
rag.retrieval.similarity-threshold=0.7

rag.ingestion.chunk-size=800
rag.ingestion.chunk-overlap=100

rag.embedding.model-name=text-embedding-v4

rag.rewrite.model=test-model